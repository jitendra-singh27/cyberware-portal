{
  "name": "cyberaware-portal",
  "version": "1.0.0",
  "description": "Cybersecurity Awareness Portal — MCA Project MCSP-232 by Shatrudhan Kumar",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
